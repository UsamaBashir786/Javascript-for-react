// 🧪 PRACTICE TASK

// Object task = {id:1, title:"Learn JS", done:false}

// Destructure → id aur title print karo

const task = {id:1, title:"Learn JS", done:false}
const {id,title,done} = task
console.log("Id :" +id,"\nTitle :" + title)