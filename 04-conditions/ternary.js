// 📂 04-conditions/ternary.js
// 🧠 MINDSET (IMPORTANT)

// React me JSX ke andar:

// if / else directly use nahi hota

// Ternary operator use hota hai

// React UI = condition ? UI : UI

const isLoggedIn = true
const message = isLoggedIn? "Welcome" : "Please Login";
console.log(message)
